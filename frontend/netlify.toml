[build]
  command = "npm run build"
  publish = "dist"

# React Router uses client-side routing, so every path needs to
# fall back to index.html or Netlify will 404 on refresh/deep links.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
