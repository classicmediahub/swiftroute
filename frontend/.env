VITE_API_URL=https://swiftroute-kguz.onrender.com/api
# Same Mapbox token as backend's MAPBOX_ACCESS_TOKEN (Mapbox's public tokens
# work fine for both server-side and browser map rendering). Needed for the
# live delivery map to render.
VITE_MAPBOX_TOKEN=
